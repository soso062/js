let titulo = document.getElementById("titulo");

titulo.innerHTML = "clique aqui pra saber o seu imc";

function imc(p, a){

    alert(p/(a*a)); 
    let resultado = document.getElementById
    ('resultado')
    resultado.innertext =(peso / (altura*altura))
}